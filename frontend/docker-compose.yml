services:
  frontend-app:
    image: talel12/front-session18
    container_name: frontend-app  # Name of the container
    environment:
      - VITE_API_URL=https://api.194.lebondeveloppeur.net/
    ports:
      - '5000:80'  # Map the container's port 3001 to the host machine's port 3001